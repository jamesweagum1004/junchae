# TODO

- Implement admin category, site, ad, and inter-ad CRUD after the database schema is confirmed.
- Add authenticated API routes for create, update, and delete operations.
- Add real logo and ad image upload endpoints after upload storage rules are confirmed.
- Persist admin account settings on the server instead of browser localStorage.
- Add deployment-specific `/api` proxy or backend routing so Apache does not serve API source files directly.
